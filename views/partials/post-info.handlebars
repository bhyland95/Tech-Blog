<div class="card">
    <div class="card-header">
       <h2><a class="link" href="/post/{{id}}">{{title}}</a></h2>
        <p class="card-body">{{post_text}}</p>

    </div>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>

</div>